//while
/*Leia números do usuário com um laço while e vá acumulando a soma. Quando o usuário
digitar 0, pare e exiba a soma total.
 Inicialize a soma em 0 e leia o primeiro número antes do início do laço.*/

 const prompt = require('prompt-sync')()
let num; 
let soma = 0; 

num = Number(prompt('Digite um número:'))
 while(){
     
 }
 
 console.log()